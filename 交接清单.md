# 交接清单（给接入方的一页纸）

> 完整说明见 `README.md`，本文件是极简速查。

## 1. 拷什么

整个 `component/agent-message/` 目录。核心就 3 个前端文件 + 1 个适配层：

| 文件 | 必选 | 作用 |
|------|------|------|
| `agent-message.css` | 必选 | 组件样式（自带 `:root` 默认主题） |
| `agent-message.js` | 必选 | 渲染组件 `AgentChat` |
| `liquid-orb.js` | **必选** | 液态球动效（漏引则思考区无动效） |
| `pi-agent-adapter.js` | 接 pi-agent 时必选 | `AgentEvent` → 组件消息 |

`server.mjs` 是参考后端（Node 版，需放进 pi-agent 工程运行）；`demo.html` 是前端最小示例。

## 2. 引入顺序（按此顺序）

```html
<link rel="stylesheet" href="agent-message.css">
<script src="liquid-orb.js"></script>   <!-- 必选 -->
<script src="agent-message.js"></script>
<script src="pi-agent-adapter.js"></script>   <!-- 接 pi-agent 时加 -->
```

## 3. 最小骨架

```js
var chat = new AgentChat(listEl, {
  toast:          function (m)     { /* 提示条 */ },
  onRegenerate:   function (round) { /* 重新生成：重跑 loop / 发 {type:'regenerate'} */ },
  onRetry:        function (round) { /* 工具重试：同重新生成 */ }
  // onFollowChange: function (f)   { /* 是否贴近底部，可选 */ }
});
```

## 4. 喂消息

```js
生产者每 emit 一条  →  chat.enqueue(msg);   // 同一 id 重复喂 = 原地流式更新
一轮结束           →  chat.completeCurrentRound();
停止生成           →  chat.abortCurrentRound();
```

## 5. 接 pi-agent

```js
var emit = makePiAgentAdapter(chat);   // AgentEvent → 组件消息
// 路径 A（进程内直连）：runAgentLoop(prompts, ctx, config, emit, signal, streamFn)
// 路径 B（WebSocket）：ws.onmessage = function (e) { emit(JSON.parse(e.data)); }
//                     后端用 Agent.subscribe 转发事件（参考 server.mjs）
```

## 6. 三个宿主回调要自己实现

组件只渲染按钮/提示入口并回调，**业务动作归宿主**：

- `toast(m)` —— 复制成功/失败、停止等提示展示。
- `onRegenerate(round)` —— 「重新生成」点下后干什么（重跑 loop 或让后端 continue）。
- `onRetry(round)` —— 工具卡「重试」同上。

## 7. 必知要点

- **`liquid-orb.js` 必引**：WebGPU 液态球；WebGPU 不可用时自动降级 2D 粒子球（内部自动完成，无需写降级代码）。
- **消息协议**：`text` / `thinking` / `toolCall`（`toolType` = builtin/skill/mcp）/ `toolResult` / `file` / `agentSummary`。同一轮共享 `groupId`；最后一个 `text` 是最终回答；`agentSummary` 触发中间过程折叠。
- **换肤**：宿主定义同名 CSS 变量覆盖（`--accent-1/2/3`、`--text-1/2/3` 等 15 个）。
- **换文案**：构造时 `opts.text = { callBuiltin: '...', ... }` 浅合并覆盖默认中文（清单见 `agent-message.js` 顶部 `DEFAULTS_TEXT`）。
